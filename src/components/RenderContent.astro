---
import type { CollectionEntry } from 'astro:content';

type Props = CollectionEntry<'posts'> | CollectionEntry<'pages'>;
const post = Astro.props;
const { Content } = await post.render();
---

<Content />
